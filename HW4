const person = {
  firstName: "John", 
  lastName: "Doe", 
  age: 3,
  
  getInfo() {
    for (let key in this) {
      if ( typeof this[key] !== 'function') {
        console.log(`${key}: ${this[key]}`);
      }
    }
  }
};
person.getInfo();
person.gender = 'man';
person.getInfo();
